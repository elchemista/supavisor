[package]
name = "service_runner"
version = "0.1.0"
edition = "2021"
[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
zip = { version = "2.4.2", default-features = false, features = ["deflate"] }
libc = "0.2"
